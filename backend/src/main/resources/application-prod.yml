spring:
  datasource:
    url: jdbc:h2:mem:proddb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: false
  sql:
    init:
      data-locations: classpath:db/data-prod.sql

management:
  endpoint:
    health:
      show-details: never